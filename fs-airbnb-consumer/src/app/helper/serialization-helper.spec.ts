import { SerializationHelper } from './serialization-helper';

describe('SerializationHelper', () => {
  it('should create an instance', () => {
    expect(new SerializationHelper()).toBeTruthy();
  });
});
